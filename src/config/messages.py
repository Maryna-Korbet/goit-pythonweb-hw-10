contact_schema_first_name = {
    "en": "First name:",
}

contact_schema_last_name = {
    "en": "Last name:",
}

contact_schema_email = {
    "en": "Email:",
}

contact_schema_phone = {
    "en": "Phone:",
}

contact_schema_birthday = {
    "en": "Birthday:",
}   

contact_schema_additional_info = {    
    "en": "Additional info:",
}

contact_schema_created_at = {
    "en": "Created at:",
}

contact_schema_updated_at = {
    "en": "Updated at:",
}

contact_search_description = {
    "en": "Search contacts by name, last name, or email (Query parameters)",
}

contact_not_found = {
    "en": "Contact not found",
}

contact_delete = {
    "en": "Contact deleted",
}

contact_update = {
    "en": "Contact updated",
}

contact_create = {    
    "en": "Contact created",
}

contact_get = {    
    "en": "Contact retrieved",
}

contact_get_all = {    
    "en": "Contacts retrieved",
}

contact_get_upcoming_birthdays = {    
    "en": "Contacts with upcoming birthdays retrieved",
}

contact_get_by_birthday = {    
    "en": "Contacts with birthdays retrieved",
}   

contact_get_by_email = {    
    "en": "Contacts with email retrieved",
}

contact_get_by_name = {    
    "en": "Contacts with name retrieved",
}

contact_get_by_last_name = {
    "en": "Contacts with last name retrieved",
}

contact_get_by_id = {    
    "en": "Contact retrieved",
}

contact_get_by_phone = {    
    "en": "Contacts with phone retrieved",
}

contact_get_by_additional_info = {    
    "en": "Contacts with additional info retrieved",
}

contact_get_by_birthday_range = {    
    "en": "Contacts with birthdays retrieved",
}

contact_get_by_email_range = {    
    "en": "Contacts with emails retrieved",
}

contact_get_by_name_range = {    
    "en": "Contacts with names retrieved",
}

contact_get_by_last_name_range = {    
    "en": "Contacts with last names retrieved",
}

contact_get_by_phone_range = {    
    "en": "Contacts with phones retrieved",
}

contact_get_by_additional_info_range = {    
    "en": "Contacts with additional info retrieved",
}

contact_get_by_id_range = {    
    "en": "Contacts with IDs retrieved",
}

contact_birthday_description = {
    "en": "Birthday (YYYY-MM-DD)",
}

contact_additional_info_description = {
    "en": "Additional info:",
}


